<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/public/css/style.css">
</head>
<body>
    <!-- Container untuk halaman registrasi -->
    <div class="container d-flex justify-content-center align-items-center vh-100">
        <div class="form-card border shadow p-4 rounded">
            <div class="text-center mb-3">
                <img src="/public/images/Anbatukang.png" alt="Logo" class="logo mb-4">
                <h2>Sign Up</h2>
                <p class="text-muted">Isi data untuk membuat akun baru  </p>
            </div>
            <form action="<%= url %>register/save" method="POST">
                <!-- Input username -->
                <div class="mb-3">
                    <input type="text" name="username" class="form-control" placeholder="Username" required>
                </div>
                <!-- Input password -->
                <div class="mb-3">
                    <input type="password" name="pass" class="form-control" placeholder="Password" required>
                </div>
                <!-- Link Sign In di atas tombol Sign Up -->
                <p class="mt-3 text-center">Already have an account? <a href="<%= url %>login">Sign In</a></p>
                <!-- Tombol sign up -->
                <button type="submit" class="btn center-btn">Sign Up</button>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
